<?php
// echo "we are connected";
$quoteArray = $_POST['quotePlaylist'];
// $quoteArray = json_decode($quoteArray);
// foreach ($quoteArray as $key) {
echo json_encode($quoteArray);
    // echo json_encode($quoteArray);
// }

